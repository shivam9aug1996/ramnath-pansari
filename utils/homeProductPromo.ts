/**
 * Kill switch for home product promo bubble.
 * Set to false (or comment out <HomeProductPromo />) to disable.
 * Works on native + web (web uses HTML5 video).
 */
export const ENABLE_HOME_PRODUCT_PROMO = true;
